# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# VS Code
.vscode/

.env
.env.*

# Secrets — never commit these. (google-services.json / GoogleService-Info.plist
# are client config and ARE tracked on purpose; the sending credential is the
# service-account JSON below, which lives only in Supabase secrets.)
*-firebase-adminsdk-*.json
service-account*.json
serviceAccount*.json
# Apple push / signing keys
*.p8
AuthKey_*.p8
*.p12
*.mobileprovision

# Flutter metadata
.metadata
.flutter-plugins

# Claude Code memory (local AI assistant state)
.claude/

# Desktop admin console (local-only tool; not part of the app build)
desktop_admin/