name: padel_clay
description: Padel Egypt — Clay Court design (Flutter scaffold generated from the prototype).
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  cupertino_icons: ^1.0.6
  supabase_flutter: ^2.9.0
  url_launcher: ^6.3.0
  flutter_web_auth_2: ^4.0.0

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
